# Runtime dependencies for the ingest pipeline.
#
# Only direct dependencies are pinned. Transitive ones (pandas, numpy,
# curl_cffi, ...) are left to pip, deliberately: this venv is built on Windows
# for development and on Linux inside the container, and pinning the full
# closure would pin platform-specific wheels that do not exist on the other.

yfinance==1.5.2
lxml==6.1.1

# AES-256-GCM, so the scheduled job can read the IBKR token the web layer
# encrypted. Without it, syncing only works when someone clicks a button.
cryptography==46.0.4
